# PLAN.md — The pi-myself Blueprint

This file is the canonical record of what we decided and why. If a later change contradicts it, update this file in the same commit. Status markers: ✅ decided · 🔜 planned · ⏸ deferred.

## 1. Goal

**pi-myself** = a pi coding-agent harness built AROUND mattpocock/skills as the process core.

- **Core (process)**: Matt Pocock's 25 promoted skills, consumed **verbatim** — never fork their text. Upgrades must stay cheap.
- **Harness (runtime)**: pi extensions, task-agent roles, slash prompts, tests — ported from `pikit-template/` (heyhuynhgiabuu/pikit) and rebuilt only where Matt's skills need pi-specific support.

Two prior repos live in this workspace for reference: `skills/` (vendored upstream clone, relocated to `vendor/mattpocock-skills/`) is now PART of pi-myself; `pikit-template/` is reference-only source material for the harness.

## 2. Decisions ✅

| # | Decision | Choice |
|---|---|---|
| D1 | Work tracking | **Matt's tracker wins.** Issue tracker via `/skill:setup-matt-pocock-skills` config (`docs/agents/issue-tracker.md`, GitHub / GitLab / local `.scratch/<feature>/issues/`). The pikit `.pi/artifacts/{TODO,PLAN,PROGRESS,DECISIONS}.md` system is **dropped** as canonical work tracking. Keep `.pi/MEMORY.md` (memory layer, different job). |
| D2 | Upstream distribution | **Path A — vendored**: `vendor/mattpocock-skills/` is the upstream git clone living in this repo; `scripts/sync-skills.mjs` fast-forwards it, hashes all 25 promoted skills into `skills-lock.json`, regenerates prompt wrappers. Never edit `vendor/mattpocock-skills/**` (including prose patches); pi-side adaptations live in the harness layer only. |
| D3 | Subagents | **Use pi-task as-is** (`npm:@heyhuynhgiabuu/pi-task`); we own only `pi.skills` role overrides (`.pi/agents/`). Revisit native delegation only if pi ships one. |

### Consequences (binding)

- pikit skills that overlap Matt's are **dropped**, not merged alongside: `grilling`, `test-driven-development`, `improve-codebase-architecture`, `planning-and-task-breakdown`, `code-review-and-quality`, `debugging-and-error-recovery`, `documentation-and-adrs`, `cloudflare`.
- pikit prompts that own a process (`/create`, `/plan`, `/ship`, `/research`) are **dropped**; Matt's flow is the only process narrative (`/grill-with-docs` → `/to-spec` → `/to-tickets` → `/implement`, detours `/wayfinder`, `/triage`, `/diagnosing-bugs`; router = `ask-matt`). Harness prompts that remain: `/verify`, `/fix`, `/init`.
- One flow, one vocabulary. APPEND_SYSTEM.md routes to Matt's skill names only.

## 3. Three-tier state boundary ✅

| Tier | Owner | Contents |
|---|---|---|
| **Domain** | Matt's conventions | `CONTEXT.md` (shared language), `docs/adr/`, `docs/agents/{issue-tracker,domain,triage-labels}.md` (per-repo, generated by `/skill:setup-matt-pocock-skills`) |
| **Work tracking** | Matt's issue tracker | specs, tickets, blocking edges, triage roles; local default `.scratch/<feature>/issues/` (gitignored is fine — disposable units) |
| **Harness** | pi-myself | `.pi/MEMORY.md` (durable distilled knowledge), `recall` over session JSONL history, safety audit, session helpers |

Rule: MEMORY.md is distilled knowledge, not a copy of CONTEXT.md vocabulary or of tracker state; if something belongs upstream, move it there.

## 4. Architecture

```
pi-myself/
├── package.json            # pi package: pi.{extensions,skills,prompts}
├── PLAN.md                 # this file
├── AGENTS.md               # behavior constitution (routing names Matt's skills only)
├── PROJECT.md              # repo map (pikit pattern)
├── README.md               # install + usage
├── vendor/
│   └── mattpocock-skills/  # ⭐ VENDORED upstream mattpocock/skills — READ-ONLY by rule
│       ├── skills/engineering/…    # registered: pi.skills entry 1 (18 skills)
│       ├── skills/productivity/…   # registered: pi.skills entry 2 (7 skills)
│       └── .claude-plugin/plugin.json  # source of truth for "promoted"
├── .pi/
│   ├── skills/             # OUR layer: memory, verification-before-completion,
│   │                       #   typescript-coding-standards, api-and-interface-design,
│   │                       #   deprecation-and-migration, security-and-hardening,
│   │                       #   source-driven-development, browser-tools (+ .pi/cli helpers)
│   ├── extensions/         # safety/, dcp/ (recall), continue-after-compaction, tps,
│   │                       #   shortcut-continue, skill-tool/ ★NEW
│   ├── agents/             # pi-task roles: explore, scout, general, reviewer
│   └── prompts/            # /verify /fix /init + AUTO-GENERATED wrappers (per user-invoked skill)
├── scripts/
│   ├── sync-skills.mjs     # vendored-sync + lock + wrapper generation (--check for CI)
│   ├── run-extension-tests.mjs
│   └── install-git-hooks.mjs
├── tests/                  # hygiene gates (see §7)
├── skills-lock.json        # upstream sha + per-skill hashes
└── .github/workflows/quality.yml
```

## 5. Matt-skills → pi mapping (the gaps we close) ✅

| Gap in pi | Harness answer |
|---|---|
| No Skill tool; docs say models don't always `read` SKILL.md | **`skill-tool` extension**: registers a `skill` tool whose enum contains ONLY model-invoked skills → Matt's `Call the Skill tool with "grilling"` works verbatim; `invocation.md`'s invariant (user-invoked skills unreachable by the model) becomes harness-enforced, not prose-enforced. Unknown/user-invoked names return a message telling the human to run the wrapper. |
| User-invoked skills only reachable as `/skill:<name>` | pi's native `/skill:<name>` slash commands (`enableSkillCommands: true`; args append via `_expandSkillCommand`). The generated prompt-wrapper layer (~14 files + sync logic) was **removed 2026-08-30** as redundant — one palette entry per skill. |
| No subagents | **pi-task** + our roles: `scout` is the research sub-agent, `reviewer` is the independent read-only review used by `implement`/`code-review` closing and by any completion claim. |
| Tracker ops are freeform prose per run | ✅ **`tracker` extension** (shipped Phase 2-3): one tool, two backends. Local markdown (`list/frontier/show/create-ticket/create-map/claim/resolve/tick/status/block/comment` over `.scratch/`) and GitHub Issues (`gh-*` ops via the `gh` CLI: issues carry `**Blocked by:** #N` / `Part of: #N` body lines, triage roles and wayfinder types are labels, claim = assign @me, resolve = close with a resolution comment). Pick the backend `docs/agents/issue-tracker.md` configures. GitLab keeps its tracker doc's own workflow for now. |
| PHASE-BOUNDARIES.md needs a ~150k smart-zone awareness | ✅ **`smart-zone` extension** (shipped + verified): measures the last turn's usage against ~150k and notifies at 60%/85%/100% with the boundary-decision order from PHASE-BOUNDARIES.md (continue → /clear → /handoff → subagent → /compact). Advice only — never compacts on its own. `/smartzone` prints the current reading. Verified live in a real TUI session (`PI_SMART_ZONE_LIMIT=4000` → `Warning: smart-zone 420.9% (~17k/4k)` with the over-level guidance); thresholds, usage summation, and the env override (min 1k, garbage falls back) are unit-smoked. |
| pi has no permission system | Port pikit's `safety` extension (block/confirm on `tool_call`, audit log, `/safety`). |
| Context loss after compaction | Port `dcp/recall` + `continue-after-compaction`; APPEND_SYSTEM tells agents to `recall` before guessing. |

## 6. What we kept / dropped from pikit

**Extensions**: keep `safety/`, `dcp/`, `continue-after-compaction`, `tps`, `shortcut-continue`; drop `todo.ts` (artifacts are gone per D1) and `herdr-agent-state.ts` (Herdr-specific).

**Skills** (keep in `.pi/skills/`): `memory`, `verification-before-completion`, `typescript-coding-standards`, `api-and-interface-design`, `deprecation-and-migration`, `security-and-hardening`, `source-driven-development`, `browser-tools`. Everything overlapping Matt: dropped (list in §2).

**Prompts**: keep `/verify`, `/fix`, `/init`; drop `/create` `/plan` `/ship` `/research` `/is`; add generated wrappers.

**Pirit tests**: rehomed into our hygiene gates (§7). **Themes**: ⏸ optional, later. **DESIGN.md, sprint templates, bin/cli.js**: dropped.

## 7. Test gates 🔜

- **Upstream parity**: 25 promoted dirs ≡ `plugin.json`; dir name ≡ frontmatter name; pi name regex `[a-z0-9-]+`; description ≤ 1024 (pi limit).
- **invocation.md as tests**: every user-invoked skill has `disable-model-invocation: true`; every `Call the Skill tool with "x"` target exists AND is model-invoked; skill-tool enum = exactly the model-invoked set.
- **Lock integrity**: `skills-lock.json` hashes match working tree; `sync-skills.mjs --check` in CI.
- **Our `.pi/skills`**: description prefix/length/no-first-person (pikit's gates, ours only — never asserted against upstream).
- **Assets**: relative references inside every SKILL.md resolve (both trees).
- **Prompts**: hand-written ones discover gates from repo guidance (no assumed npm scripts, NOT DECLARED ≠ PASS, evidence contracts); wrappers fresh vs vendored set.
- **Markers**: no `FIXME:` in tracked code.
- Extension unit tests (ported + new for skill-tool).

## 8. Roadmap

- **Phase 1**: ✅ scaffold, vendored skills + lock + wrapper generation, ports (skills/extensions/agents/tests), skill-tool, docs, CI.
- **Phase 2**: ✅ `tracker` extension (local `.scratch/`, 11 ops + `/frontier`); ✅ `smart-zone` meter; ✅ `.pi/settings.json` (dogfood defaults). Remaining: dogfood pi-myself on itself with grill-with-docs → to-spec → to-tickets.
- **Phase 3**: ✅ GitHub tracker backend (`gh-*` ops in the `tracker` tool). Remaining: ⏸ themes; polish.
- **Dogfood round 1 (2026-08-29)**: ✅ the flow ran on this repo itself — grilling decisions → spec (#1) → 5 tickets (#2–6) published through the tracker tool's GitHub backend; `docs/agents/` + `CONTEXT.md` + AGENTS.md `## Agent skills` block written; labels `needs-triage/needs-info/ready-for-agent/ready-for-human/wontfix` created. Found: gh-frontier shows parent/spec issues as takeable (ticket #6). The repo now tracks its own work ON the process it was built to serve.
- **Phase 4**: ✅ initial commit (`d0af56c`); ✅ verified in a real pi turn (0.84.3, headless): `skill`/`tracker`/`recall`/`task` registered, both backends dispatch (`tracker list` → empty-tracker message; `gh-list` surfaces the gh no-remote error cleanly), vendored + local skills both discoverable, and the skill tool refuses user-invoked skills live (told the human to run `/wayfinder`). ⏸ themes dropped by decision (2026-08-29). Remaining: publish (git remote + `pi install git:…`), dogfood, tests debt.
- **Invocation-surface dedupe (2026-08-30, user decision)**: searching "setup" in the palette surfaced both the generated wrapper `/setup-matt-pocock-skills` and pi's native `/skill:setup-matt-pocock-skills` — two entries for one SKILL.md. Chose native-only: deleted the 14 AUTO-GENERATED wrappers and the wrapper machinery in sync-skills.mjs plus its test; `enableSkillCommands: true` in `.pi/settings.json` is now the single user-invocation surface (native expansion appends args like the wrappers did — `_expandSkillCommand`). `argument-hint` display hints are lost (cosmetic); hand-written prompts (`verify`, `fix`, `init`, `remember`, `setup-pi-myself`) keep typed names.
- **Global-skill cleanup + roster completion (2026-08-30)**: the 25 ad-hoc global copies of Matt's skills (`~/.pi/agent/skills/`) duplicate the vendored, version-locked set — deleted so pi-myself is the single source. Also deleted: `_pi-task/REFERENCE.md` (leftover from the previous harness generation — stale roster incl. the dropped `builder`, `/skill:` syntax of another host; nothing in the pi-task package references it) and the 6 duplicate global agent files; the `ultra-review`/`ultra-review-receive` codex toolchain (skills + its 2 agents) is kept by decision — **superseded same day (2026-08-30, user request)**: the pair moved INTO pi-myself (`.pi/skills/ultra-review{,-receive}/` as user-invoked local skills with the house routing-prefix descriptions, agent defs as `ultra-scout`/`ultra-verifier` roles; the hardcoded host model names dropped in favor of the roster standard `opencode-go/deepseek-v4-flash`, `$ROOT`-relative report script, `/name` invocation syntax), and the global symlinks + agent files were removed — global now holds no skills or agents; the codex originals under `~/.codex/skills/` are untouched. `test-proof-debt-audit` (exists nowhere upstream; the only copy was global) migrated into `.pi/skills/` (user-invoked, description rewritten to the house routing-prefix convention). Agent roster gaps closed against the skills' actual delegation demands: `codebase-design` DESIGN-IT-TWICE needs parallel read-only design sub-agents (no existing role fit) → added `designer`; wayfinder research tickets want a child that writes the cited findings artifact ("capture findings on a throwaway branch") → added `researcher` (skills: `research` + `source-driven-development`, one report path each — the sole WIP-cap carve-out). `builder` dropped (≡ `general`).
- **Latent coupling the cleanup exposed (fixed same day)**: pi-task resolves agent frontmatter `skills:` via pi's resource loader with `noExtensions: true`, which never sees package.json `pi.skills` (the extension/package flow) — the roles only ever resolved because the deleted global copies existed. Project settings `skills` paths resolve relative to `.pi/` (the config dir), so repo-root-relative vendor paths silently fail; the fix in `.pi/settings.json` uses `../vendor/mattpocock-skills/skills/{engineering,productivity}`. Verified: all four skill-declaring roles resolve from repo root, a live `reviewer` task launches, and normal sessions show no duplicate-skill diagnostics (pi canonical-path dedupe). Root 13-test suite also repaired: the extension-layout guard banned support dirs without `index.ts` (safety/ is support for the top-level `safety.ts` wrapper and must NOT have index.ts — pi would load it as a second extension); the test now pins the known support-dir list instead.
- **Installed-layout verification in a fresh repo (2026-08-30)**: `pi install git:github.com/MinhDuyDEV/pi-myself -l` into a scratch repo, then probes. Task-child skill resolution WORKS in installed layout with no extra config (project settings `packages` lets PackageManager resolve the package manifest's `pi.skills`, which survives pi-task's `noExtensions` loader — the checkout's settings.json fix above covers only the checkout layout). Task ROLES do not: pi-task `discoverAgents` scans bundled + user + project dirs only, so the harness-authored roles are invisible in a fresh repo. Fix shipped: `scripts/setup-agents.mjs` (idempotent package→project copy of `.pi/agents/*.md` with frontmatter, drift-refreshing) plus the `/setup-pi-myself` prompt; test coverage in `tests/setup-agents.test.ts` (provision set, idempotency, user-edit protection). The README's install flow now includes `/setup-pi-myself` as the post-install step beside `/skill:setup-matt-pocock-skills`.

## 9. Deferred / open ⏸

- Whether `.scratch/` state gets its own recall provenance (needs dogfooding).
- Themes (catppuccin/tokyo-night) — port on request.
- GitHub Issues tracker backend for the future `tracker` extension.
- Web-research package choice (decided 2026-08-29): name-agnostic by design — this repo's host runs `pi-web-access` (`web_search`/`get_search_content`/`fetch_content`, incl. `source_check` for citations); `pi-search` (`websearch`/`codesearch`/`web_fetch`/`context7`/`deepwiki`) is the README's optional alternative. Both covered in APPEND_SYSTEM + scout; never install both alongside each other.
- `pi-review-loop`, `pi-pretty`, `pi-diff` — third-party QoL, not wired by default.